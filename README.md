To run the project you should run watch the steps given below.

- The project has virtual environment, so first run venv\Scripts\activate
- To activate the venv run this command: source venv/bin/activate
- Another requierement of the project is flask. In case if in your environment doesn't have flask upload it: pip install flask
- After all the commands run, to start the project run * python app.py *

